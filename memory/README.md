# Memory data directories

- `sqlite/` — SQLite database file (gitignored)
- `vectors/` — LanceDB tables (gitignored)

Created at runtime in Phase 4. Keep the `.gitkeep` files so the folders exist in a fresh clone.
